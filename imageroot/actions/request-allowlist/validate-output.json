{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "request-allowlist output",
    "$id": "http://schema.nethserver.org/crowdsec/request-allowlist-output.json",
    "description": "Result of an allowlist request to nethesis-insights",
    "examples": [
        {
            "accepted": true,
            "requests": 1
        }
    ],
    "type": "object",
    "required": ["accepted", "requests"],
    "properties": {
        "accepted": {
            "type": "boolean",
            "title": "accepted",
            "description": "Whether the request was recorded (also true when the CIDR was already allowlisted)"
        },
        "requests": {
            "type": "integer",
            "title": "requests",
            "description": "Current distinct-system count requesting this CIDR"
        }
    }
}
